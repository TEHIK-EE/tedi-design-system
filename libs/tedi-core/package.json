{
  "name": "@tehik-ee/tedi-core",
  "version": "0.0.0-semantic-version",
  "main": "src/index.scss",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -a src/* dist/ && cp README.md dist/ && cp package.json dist/",
    "release": "npx semantic-release -e semantic-release-monorepo --debug"
  },
  "publishConfig": {
    "access": "public"
  }
}
