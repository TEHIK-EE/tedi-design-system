{
  "name": "@tehik-ee/tedi-core",
  "version": "1.0.0",
  "main": "src/index.scss",
  "scripts": {
    "build": "sass src/index.scss dist/index.css --style=compressed && cp README.md dist/ && cp package.json dist/",
    "release": "npx semantic-release -e semantic-release-monorepo --debug"
  },
  "devDependencies": {
    "sass": "^1.83.1"
  }
}
