{
  "name": "@tehik-ee/tedi-core",
  "version": "0.0.0-semantic-version",
  "main": "src/index.scss",
  "scripts": {
    "build": "sass src/index.scss dist/index.scss && cp README.md dist/ && cp package.json dist/",
    "release": "npx semantic-release -e semantic-release-monorepo --debug"
  },
  "devDependencies": {
    "sass": "^1.83.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
