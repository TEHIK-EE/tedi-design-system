stages:
  - publish

default:
  image: docker.repo.tehik.ee/node:lts-bullseye-slim
  before_script:
    - npm ci --cache .npm --prefer-offline
    - |
      {
        echo $NPM_NPMRC_BASE64 | base64 -d
        echo "@tehik:registry=${ARTIFACTORY_NPM_API_URL}${NPM_REPO_KEY_RELEASE}"
        echo "__NPM_AUTH_TOKEN__=${CI_JOB_TOKEN}"
        echo "__NPM_REGISTRY__=${ARTIFACTORY_NPM_API_URL}${NPM_REPO_KEY_RELEASE}"
        echo "__NPM_EMAIL__=$NPM_EMAIL"
      } | tee -a .npmrc
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - .npm/

publish-library:
  stage: publish
  script:
    - npm run build
    - cd ./dist/packages/components
    # - npm run semantic-release
    - npm publish
  #TODO - npm publish
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
