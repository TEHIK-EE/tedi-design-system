import {Meta, Title, ArgsTable, Primary, Stories} from '@storybook/blocks';
import * as SkeletonStories from './skeleton.stories.tsx';

<Meta of={SkeletonStories} />

<Title />

Skeleton is a component that is used to indicate that content is loading.
Build your skeleton using the SkeletonBlock component.

<Primary />

<ArgsTable />

<Stories includePrimary={false} />
