import { Meta, Title, Canvas, ArgsTable, Stories, Description } from '@storybook/blocks';
import * as SeparatorStories from './separator.stories.tsx';

<Meta of={SeparatorStories} />

<Title />
<p>
  Used to visually separate or divide sections or content within a user interface. It helps create a clear visual
  distinction between different elements, providing structure and improving the overall readability and organization of
  the interface. They can be used vertically and horizontally.
</p>

<Canvas of={SeparatorStories.Default} />

<ArgsTable />

<Stories includePrimary={false} />
