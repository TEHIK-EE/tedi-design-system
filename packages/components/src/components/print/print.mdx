import { Meta, Title, Canvas, Controls, Stories } from '@storybook/blocks';
import { Description } from '@storybook/addon-docs';
import * as PrintStories from './print.stories.tsx';
import { Notification } from '../notification/notification';

<Meta of={PrintStories} />

<Title />
<p>
  Print component helps with modifying UI during printing. This can also be achieved using printing utility classes, but
  using this component is <b>preferred way</b>.
</p>

<Notification>
  Print direct children cannot be component that returns some conditional logic. workaround is to move Print component
  up or down inside component tree! Or wrap children with div.
</Notification>

<Canvas of={PrintStories.Default} />

<Controls />

<Stories includePrimary={false} />
