import { Meta, Title, Canvas, ArgsTable, Stories } from '@storybook/blocks';
import * as HeaderLanguageStories from './header-language.stories.tsx';

<Meta of={HeaderLanguageStories} />

<Title />
HeaderLanguage return a list of languages in HeaderDropdown. Pass it as direct children of Header and it will be automatically
positioned. <br />
**Note**: This component is not meant to be used outside of Header.

<Canvas of={HeaderLanguageStories.Default} />

<ArgsTable />

<Stories includePrimary={false} />
