import { Meta, Title, Canvas, ArgsTable, Stories } from '@storybook/blocks';
import * as HeaderRole from './header-role.stories.tsx';

<Meta of={HeaderRole} />

<Title />
HeaderRole is predefined component to handle showing user role and allow to change it. <br />
**Note**: This component is not meant to be used outside of Header.

<Canvas of={HeaderRole.Default} />

<ArgsTable />

<Stories includePrimary={false} />
