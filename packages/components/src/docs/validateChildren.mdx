import { Meta, Title, Subtitle, Unstyled } from '@storybook/blocks';
import { Text, VerticalSpacing } from '../index.ts';

<Meta name="Validate Children" title="Documentation/Validate Children" />

# How to validate the children prop of any component

Typically React and TypeScript allow for very strict typing, however this is not true for the children prop of a component.

TODO: Example of how it might seem logical, and what the result is

Examples of how the validation works: see validateChildren.spec.tsx
